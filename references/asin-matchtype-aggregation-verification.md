# ASIN匹配类型聚合验证

真实广告分析任务390001此前包含56个原始匹配类型值，其中多个具体ASIN和自动投放子类型被错误拆分。修复后，匹配类型Tab计数为5，环形图和占比列表仅展示精准匹配、广泛匹配、ASIN匹配、自动匹配和短语匹配。

当前真实占比分别为：精准匹配57.0%、广泛匹配14.9%、ASIN匹配9.9%、自动匹配9.4%、短语匹配8.8%。合理性判断与图表、列表一致，仍显示精准匹配57.0%。
